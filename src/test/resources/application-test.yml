spring:
  flyway:
    schemas: public
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:p6spy:postgresql://localhost:5433/db_test
    username: test
    password: test
  jpa:
    properties:
      hibernate:
        default_schema: public
